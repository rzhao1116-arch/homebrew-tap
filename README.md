# homebrew-tap

Homebrew tap for `rzhao1116-arch` tools.

## Install

```bash
brew tap rzhao1116-arch/homebrew-tap
brew install codex-agents
```

Or without a separate tap step:

```bash
brew install rzhao1116-arch/homebrew-tap/codex-agents
```
